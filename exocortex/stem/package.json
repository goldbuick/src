{
  "name": "stem",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src/ -d build -s",
    "stem-mesh": "npm run build && node build/stem-mesh.js",
    "stem-config": "npm run build && node build/stem-config.js"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "rethinkdb": "^2.3.3",
    "seneca": "^3.1.0",
    "seneca-balance-client": "^0.6.0",
    "seneca-mesh": "^0.9.0",
    "slate-irc": "^0.9.0"
  }
}
