{
  "name": "stem",
  "version": "1.0.0",
  "scripts": {
    "stem-facade": "npm run build && node build/stem-facade.js",
    "stem-codex": "npm run build && node build/stem-codex.js",
    "stem-irc": "npm run build && node build/stem-irc.js",
    "build": "babel src/ -d build -s"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "seneca": "^3.1.0",
    "seneca-balance-client": "^0.6.0",
    "seneca-mesh": "^0.9.0",
    "slate-irc": "^0.9.0"
  }
}
