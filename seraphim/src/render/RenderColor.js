import * as THREE from 'three';

let RenderColor = {
    color: 
    new THREE.Color("rgb(125,163,192)")
    // new THREE.Color("rgb(250,2,60)")
    // new THREE.Color("rgb(140,180,255)")
};

export default RenderColor;
